spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trustloan
    username: root
    password: Hellobrother@19

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

server:
  port: 8080

sendgrid:
  api-key: ${SENDGRID_API_KEY}
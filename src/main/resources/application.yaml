spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trustloan_db
    username: root
    password: Hellobrother@19

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

sendgrid:
  api-key: ${SENDGRID_API_KEY}
  from-email: ${SENDGRID_FROM_EMAIL}